# Style Guide

- Use snake_case for all variable and function names.
- Add type annotations to all function signatures.
- Keep functions short — if a function exceeds 15 lines, consider splitting it.
- Use dict comprehensions and list comprehensions where they improve readability.
- Avoid mutable default arguments.
